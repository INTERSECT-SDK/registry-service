name: Helm Chart Release

on:
  push:
    branches:
      - main
    paths:
      - charts/**
      - .github/workflows/helm-release.yml
  pull_request:
    branches:
      - main
    paths:
      - charts/**
      - .github/workflows/helm-release.yml
  workflow_dispatch:
    inputs:
      publish:
        description: Publish chart artifacts to GitHub Pages via chart-releaser
        required: true
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  helm-validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"

      - name: Install Helm
        uses: azure/setup-helm@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Lint and template charts
        run: ./charts/lint-charts.sh

  helm-release:
    runs-on: ubuntu-latest
    needs: helm-validate
    if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.publish))

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"

      - name: Install Helm
        uses: azure/setup-helm@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run chart-releaser
        uses: helm/chart-releaser-action@v1.7.0
        env:
          CR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          charts_dir: charts

      - name: Sync GitHub Pages README
        run: |
          cp charts/README.md /tmp/gh-pages-readme.md
          git fetch origin gh-pages:gh-pages
          git checkout gh-pages
          cp /tmp/gh-pages-readme.md README.md
          git add README.md
          if git diff --cached --quiet; then
            echo "GitHub Pages README already up to date"
            exit 0
          fi
          git commit -m "docs: update GitHub Pages Helm README"
          git push origin gh-pages
